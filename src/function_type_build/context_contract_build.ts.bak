// okay
// pending names
// - for each, retain same contract as their origin context
// declared names
// - each are "stubbed"

import { FunctionLookUpTable } from '../function_type_build';

interface ContextContractThing {
  // (pending names too)
  // declared names into a list "stubbed" function types
  // list of things that
};
